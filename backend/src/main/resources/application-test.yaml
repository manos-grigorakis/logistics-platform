spring:
  config:
    activate:
      on-profile: test

  security:
    user:
      name: testuser
      password: testpass123

  jwt:
    expiration-ms: 60000 # 1m

  datasource:
    url: "jdbc:h2:mem:testdb"
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        hbm2ddl:
          auto: create-drop

  flyway:
    enabled: false

  mail:
    host: localhost
    port: 1025
    username: test
    password: test

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

logging:
  level:
    root: INFO

jwt:
  secret: "dGhpcy1pcy1qd3QtdGVzdC1zZWNyZXQtbG9uZy1rZXktMTIzNDU2"

swagger:
  enabled: false

management:
  endpoint:
    health:
      probes:
        enabled: false
  health:
    defaults:
      enabled: false

app:
  frontend:
    url: "http://localhost:4200"
  setup_password:
    expires: "20m"
  reset_password:
    expires: "20m"

  platform:
    display_name: "Logistics Platform"
    no_reply_mail: "Logistics Platform <no-reply@logistics.com>"
    support_mail: "Support <support@logistics.com>"
    primary_dark_color: "#0f172a"
    accent_light_color: "#3498db"

  aws:
    region: "eu-central-1"

  minio:
    accessKey: admin
    secretKey: admin
    endpoint: "http://localhost:9001"
    bucketName: "my_bucket"
    bucketPathQuotes: "quotes/"
    bucketPathCmr: "cmr/"
    bucketPathReconciliationReport: "reconciliation-report/"
    bucketPathSuppliers: "suppliers/"
    bucketPathCompanyProfile: "company-profile/"

tax:
  vat: 24