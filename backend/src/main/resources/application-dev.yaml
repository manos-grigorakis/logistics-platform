spring:
  config:
    activate:
      on-profile: dev

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

logging:
  level:
    root: INFO
    com.manosgrigorakis.logisticsplatform: DEBUG

swagger:
  enabled: true